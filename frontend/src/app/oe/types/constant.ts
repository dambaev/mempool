export enum ArrowDirections {
  LEFT= 'ArrowLeft',
  RIGHT = 'ArrowRight',
}

export enum BlockTypes {
  STRING = 'string',
  NUMBER = 'number',
}

export enum BlockParts {
  PRE_HASH = 'blocktxs-',
}
